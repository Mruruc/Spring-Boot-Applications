package com.mruruc.component_annotation.config;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Import;

//@Configuration
//@Import(Sort_Config.class)
public class Sort_Config2 {
 /*   private SortingAlgorithm sortingAlgorithm;
    @Autowired
    public Sort_Config2(@Qualifier("bubbleSort")SortingAlgorithm sortingAlgorithm1){
        this.sortingAlgorithm=sortingAlgorithm1;
    }

    @Bean
    public void sort(){
         sortingAlgorithm.sort();
    }*/



}
